  <nav class="navbar navbar-expand-lg bg-light">
  <div class="container">
    <%= link_to "Who's Free?", root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Blocks', blocks_path, class: "nav-link #{active_class(blocks_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Teachers', teachers_path, class: "nav-link #{active_class(teachers_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Departments', departments_path, class: "nav-link #{active_class(departments_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to 'Classrooms', classrooms_path, class: "nav-link #{active_class(classrooms_path)}" %>
        </li>
      </ul>
      <ul class="navbar-nav float-end">
        <% if user_signed_in? %>
          <%= link_to 'Log Out', destroy_user_session_path, class: 'nav-link', data: { turbo_method: :delete } %>
        <% else %>
          <%= link_to 'Log In', new_user_session_path, class: 'nav-link' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
