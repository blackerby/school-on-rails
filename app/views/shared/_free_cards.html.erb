<% objects.each do |object| %>
  <div class="card">
    <div class="card-body">
      <h5 class="card-title">
        <%= link_to object.name, object, data: { turbo_frame: '_top' } %>
      </h5>
    </div>
  </div>
<% end %>
