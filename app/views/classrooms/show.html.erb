<div class="col-lg-12 text-center">
  <h1>Classroom <%= @classroom.name %></h1>
  <div class="container text-center">
    <%= link_to 'Edit', edit_classroom_path(@classroom) %>
    <%= link_to 'Delete', @classroom, 
                data: { turbo_method: :delete, 
                        turbo_confirm: "Are you sure you want to delete #{ @classroom.name }?" } %>
  </div>
</div>
<div class="container text-center">
  <div class="row">
    <div class="col">
      <h3>Teaches During</h3>
      <%# <%= render 'shared/table', objects: @blocks%>
    </div>
    <div class="col">
      <h3>Free During</h3>
      <%# <%= render 'shared/table', objects: @teacher.free %>
    </div>
  </div>
</div>
